# Docker Compose variant (app container -> db service)
DATABASE_URL_DOCKER="postgresql://postgres:postgres@db:5432/smart_finance_dashboard?schema=public"

# Manual local variant (host machine -> local postgres)
DATABASE_URL_LOCAL="postgresql://postgres:postgres@localhost:5432/smart_finance_dashboard?schema=public"

# In Docker mode this is overridden by docker-compose.yml. For manual local setup only.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/smart_finance_dashboard?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-string"
